//déclarations des variables----------------------------------------------------------------------------------

const itemNavLateral = document.querySelectorAll('.item-nav-lateral');
const article = document.querySelector('article');
let tabTask = []; // declare le tableau contenant les instances de la classe task pour le menu tache

// tempo
let tabBackupTask = []; // déclare le tableau qui va stocker le texte des taches
